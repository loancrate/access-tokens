{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".tsbuildinfo"],
      "inputs": ["src/**", "package.json", "tsconfig.json"]
    },
    "typecheck": {
      "dependsOn": ["build"],
      "outputs": [],
      "inputs": ["src/**", "package.json", "tsconfig.json"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**", "*-junit.xml"],
      "inputs": ["src/**", "vitest.config.ts"]
    },
    "test-int": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**", "*-int-junit.xml"],
      "inputs": ["src/**", "vitest-int.config.ts"],
      "cache": false
    },
    "test-smoke": {
      "dependsOn": ["build"],
      "outputs": ["*-smoke-junit.xml"],
      "cache": false
    },
    "lint": {
      "dependsOn": ["build"],
      "outputs": [],
      "inputs": [
        "src/**",
        "eslint.config.cjs",
        "../../eslint-config-base.cjs",
        "tsconfig.json"
      ]
    },
    "format": {
      "outputs": [],
      "inputs": ["src/**", "*.json", "*.md"]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    }
  }
}
